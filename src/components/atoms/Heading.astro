---
// Heading Atom Component
// This is a basic building block for headings
// TODO: Implement as the site is built out
// 
// Purpose: Reusable heading component with consistent typography
// Usage: <Heading level="h1" size="xl">Main Title</Heading>
// 
// Props to implement:
// - level: 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6'
// - size: 'xs' | 'sm' | 'md' | 'lg' | 'xl' | '2xl' | '3xl' | '4xl'
// - color?: 'default' | 'primary' | 'secondary' | 'muted' | 'inverse'
// - weight?: 'light' | 'normal' | 'medium' | 'semibold' | 'bold'
// - align?: 'left' | 'center' | 'right' | 'justify'
// - className?: string (additional CSS classes)
// - children: string | AstroComponent
// 
// Design tokens to use:
// - Typography scale from design system
// - Consistent line heights
// - Color palette integration
// - Responsive sizing
// 
// Accessibility requirements:
// - Proper heading hierarchy (h1 > h2 > h3, etc.)
// - Screen reader support
// - Color contrast compliance
// - Focus management for interactive headings
---

<div class="p-4 border-2 border-dashed border-gray-300 rounded-lg bg-gray-50">
  <h3 class="text-lg font-semibold text-gray-700 mb-2">Heading Atom</h3>
  <p class="text-sm text-gray-600 mb-4">
    This is a placeholder for the Heading atom component. It should be implemented as the site is built out.
  </p>
  <div class="space-y-2">
    <p class="text-xs text-gray-500"><strong>Purpose:</strong> Reusable heading component with consistent typography</p>
    <p class="text-xs text-gray-500"><strong>Usage:</strong> &lt;Heading level="h1" size="xl"&gt;Main Title&lt;/Heading&gt;</p>
    <p class="text-xs text-gray-500"><strong>Status:</strong> 🚧 Placeholder - Implementation needed</p>
  </div>
</div>
