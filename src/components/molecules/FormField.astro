---
// FormField Molecule Component
// This is a simple combination of atoms for form inputs
// TODO: Implement as the site is built out
// 
// Purpose: Reusable form field combining Label, Input, and error handling
// Usage: <FormField label="Email" type="email" required error="Invalid email" />
// 
// Props to implement:
// - label: string (field label)
// - type: 'text' | 'email' | 'password' | 'number' | 'tel' | 'url' | 'search'
// - name: string (field name)
// - value?: string (field value)
// - placeholder?: string (field placeholder)
// - required?: boolean (required field indicator)
// - disabled?: boolean
// - readonly?: boolean
// - error?: string (error message)
// - helper?: string (helper text)
// - size: 'sm' | 'md' | 'lg'
// - variant: 'default' | 'filled' | 'outline'
// - className?: string (additional CSS classes)
// 
// Atoms used:
// - Label (for field label)
// - Input (for field input)
// - Icon (for field icons)
// 
// Design tokens to use:
// - Consistent spacing between elements
// - Error state styling
// - Success state styling
// - Focus states and transitions
// 
// Accessibility requirements:
// - Proper field labeling
// - Error announcements
// - Required field indicators
// - Screen reader support
// - Keyboard navigation
---

<div class="p-4 border-2 border-dashed border-blue-300 rounded-lg bg-blue-50">
  <h3 class="text-lg font-semibold text-blue-700 mb-2">FormField Molecule</h3>
  <p class="text-sm text-blue-600 mb-4">
    This is a placeholder for the FormField molecule component. It should be implemented as the site is built out.
  </p>
  <div class="space-y-2">
    <p class="text-xs text-blue-500"><strong>Purpose:</strong> Reusable form field combining Label, Input, and error handling</p>
    <p class="text-xs text-blue-500"><strong>Usage:</strong> &lt;FormField label="Email" type="email" required error="Invalid email" /&gt;</p>
    <p class="text-xs text-blue-500"><strong>Atoms used:</strong> Label, Input, Icon</p>
    <p class="text-xs text-blue-500"><strong>Status:</strong> 🚧 Placeholder - Implementation needed</p>
  </div>
</div>
